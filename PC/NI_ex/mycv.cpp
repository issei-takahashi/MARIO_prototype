#include "mycv.hpp"

void mycv::depthFrame2Mat( int rows, int cols, int type, void* data, cv::Mat* pDst )
{
	pDst->create(rows,cols,type);
	for(int x=0; x<cols; x++){
		for(int y=0; y<rows; y++){
			
		}
	}
}
